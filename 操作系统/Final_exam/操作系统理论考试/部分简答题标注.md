# 1.实现多道程序设计需要哪些必须的硬件设施？（CPU、内存、总线、外部存储器除外）p36
中断、特权指令、内存保护、定时器。
# 2.什么是系统调用？从操作系统角度看，它们在运行状态上与普通函数有什么区别？
第二章不知道在哪一页

系统调用：当用户程序调用操作系统所提供的函数时成为系统调用

区别：系统调用需要内核的参与，要在内核模式下执行，需要模式切换；普通函数在用户模式下执行，不需模式切换

